# AGENTS.md

The operating rules for this repo live in **CLAUDE.md** — read that file, plus
`memory/MEMORY.md` and the fact files it indexes, at the start of every session.

This file is only a pointer, so the rules can't drift into two versions. If a
tool you use reads AGENTS.md and not CLAUDE.md, copy CLAUDE.md's contents here
and keep both in sync.
